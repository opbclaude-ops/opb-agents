---
description: Вызвать Начальника (agent-boss) — оркестратор расчёта по объекту, знает каталог Drive, обёртки и историю проекта.
---

Делегируй задачу сабагенту `agent-boss`. Он сам определит сценарий
(полный расчёт / частичный pipeline / статус проекта / каталог
агентов / сверка с эталоном), вызовет нужных сабагентов через
инструмент Task, соберёт результат и отдаст артефакты через
`present_files`.

Не выполняй задачу сам — твоя роль здесь только запустить Босса с
его инструкцией из `.claude/agents/agent-boss.md`. Если у Босса
возникнет уточняющий вопрос — пробрось его пользователю.

Запрос пользователя:

$ARGUMENTS
