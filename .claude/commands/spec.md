---
description: Собрать спецификации (agent-spec) — парсит PDF слаботочки (АППЗ/СВН/ТСО/СКУД/СОУЭ/ДП/ЭОМ) в xlsx. Режимы: одна система / все системы.
---

Делегируй задачу сабагенту `agent-spec` (обёртка над навыком
`spec-collect` v1.0). Он сам определит режим (одна система / все
системы), прочитает свой SKILL.md, прогонит все 9 этапов с воротами
самопроверок, и отдаст итоговый xlsx через `present_files`.

Не выполняй задачу сам — только запусти агента. Если непонятен режим
или не хватает выгрузки `База_3_ОПБ` для матчинга номенклатуры —
пробрось вопрос пользователю.

Запрос пользователя:

$ARGUMENTS
